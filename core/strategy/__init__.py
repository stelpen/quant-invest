"""Strategy package.

Provides the abstract :class:`Strategy` base class and concrete strategy
implementations (e.g. :class:`~core.strategy.ma_cross.MACrossStrategy`)
plus the :class:`~core.strategy.screener.StockScreener` for stock filtering.
"""